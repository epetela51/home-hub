import React from 'react';
import UnknownAccordion from '../../../components/UnknownAccordian/UnknownAccordian';

/**
 * Presentational component that wraps the UnknownAccordion
 * Handles spacing and layout for the unknowns section
 */
const UnknownBreakersSection = () => {
  return <UnknownAccordion />;
};

export default UnknownBreakersSection;
